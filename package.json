{
  "name": "equiptrack-fullstack",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
  }
}